# Secrets & local config — never commit
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Node
node_modules/
dist/
worker/.wrangler/

# Logs / scratch
*.log
/tmp/
*.bak
.DS_Store

# Note: avatars/ and card-studio/static/cards/ are TRACKED on purpose —
# they are the identity store and the gallery product. No CI rebuilds them,
# so a fresh clone must ship with them.
